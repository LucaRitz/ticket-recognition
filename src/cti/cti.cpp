#include <include/cti.hpp>
#include <some/someClass.hpp>

namespace cti {
    const Matcher *const Cti::train(const vector<const Ticket> &tickets) const {
        return nullptr;
    }

    void Cti::hello(char* image) const {
        display(image);
    }
}
